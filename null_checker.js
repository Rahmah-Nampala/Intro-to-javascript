for (var i = 1; i <=10; i++) // for a program that prompts for a new number 10 times
{
	let num = prompt ("Please enter number");
	// The screen prompt should print
	if (num<0)
		console.log(-1);
	else if (num<0)
		console.log(1);
	else
		console.log(0);
}
	